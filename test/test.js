var chai = require('chai');
var expect = chai.expect;
var functions = require('../index');
var setReminder = functions.setReminder;


describe('reminds us to take lunch', function() {

  it('should tell us to go to lunch', function(done) {
  	// TODO: send real stuff to this function
  	setReminder();
  	// var timer('should go off at a certain time')
  });
});
